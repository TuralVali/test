---
name: transform-json
description: Transform JSON files using Excel mappings with validation, diff preview, smart matching, and parallel processing
model: GPT-4.1
tools:
  - codebase
---

Transform JSON files using Excel mapping rules and ensure output correctness.

Context:
- Mapping files: <provide Excel file paths>
- Input folder: <provide folder with JSON files (e.g. 2025)>
- Output folder: <provide folder for new JSON files (e.g. 2026)>
- Target version: <e.g. 2026>

---

Step 1 — Load mappings
- Read Excel files using pandas
- Normalize column names
- Merge mappings into one table

Expected columns:
- old_field
- new_field
- old_value
- new_value
- match_type (exact | regex | fuzzy)

---

Step 2 — Process JSON files
- Read all `.json` files from input folder
- Process files in parallel using concurrent.futures
- Preserve structure

---

Step 3 — Apply smart mappings

Apply in priority order:
1. Exact match
2. Regex match (use re)
3. Fuzzy match (use difflib or rapidfuzz, threshold ≥ 85%)

Requirements:
- Traverse JSON recursively
- Rename keys
- Replace values
- Preserve data types
- Do not modify unknown fields

---

Step 4 — Version transformation

- Replace "2025" with target version
- Update file names and relevant JSON fields

---

Step 5 — Generate diff preview

For each file:
- Compare original vs transformed JSON
- Generate Git-style diff:

```diff
- "old_key": "old_value"
+ "new_key": "new_value"
